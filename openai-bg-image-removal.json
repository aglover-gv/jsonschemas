{
  "type": "object",
  "properties": {
    "imageURL": {
      "type": "string",
      "description": "A publicly accesble URL to the image generated so that it can be retrieved by another system"
    },
    "message": {
      "type": "message",
      "description": "Any additional response other than the URL"
    }
  },
  "required": ["imageURL", "message"]
}